export enum HttpMethods { /* Enum to contain the allowed HTTP methods that can be used to interact with the backend system, maximising control and limiting security issues and errors */
    GET = "GET",
    POST = "POST"
}